{
  "name": "notifications-ui",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/notifications/ui/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:notifications", "type:ui"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/notifications/ui"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
